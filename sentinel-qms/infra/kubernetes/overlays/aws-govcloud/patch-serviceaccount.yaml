apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel-backend
  namespace: sentinel-qms
  annotations:
    # IRSA — bind to an IAM role granting S3 + Secrets access (least privilege).
    eks.amazonaws.com/role-arn: arn:aws-us-gov:iam::123456789012:role/sentinel-qms-prod-eks-backend
