# Base URL for the Sentinel QMS REST API.
# In development the Vite dev server proxies /api -> http://localhost:8000.
# In production nginx proxies /api to the backend service.
VITE_API_BASE_URL=/api/v1

# Optional: display the deployment environment label in the CUI banner.
VITE_DEPLOYMENT_LABEL=AWS GovCloud (US)
