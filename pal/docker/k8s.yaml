# PAL Platform — reference Kubernetes manifest (AKS / EKS / vanilla / on-prem).
# Replace the Secret values, storage class and ingress host for your environment.
apiVersion: v1
kind: Secret
metadata:
  name: pal-secrets
type: Opaque
stringData:
  JWT_SECRET: "replace_with_64_hex_chars_minimum_value"
  DATABASE_URL: "postgres://pal:password@postgres:5432/pal"
  ADMIN_EMAIL: "admin@your-domain.gov"
  ADMIN_PASSWORD: "replace_me"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pal-uploads
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pal-platform
  labels: { app: pal-platform }
spec:
  replicas: 2
  selector:
    matchLabels: { app: pal-platform }
  template:
    metadata:
      labels: { app: pal-platform }
    spec:
      containers:
        - name: pal
          image: pal-platform:latest
          ports:
            - containerPort: 80
          envFrom:
            - secretRef: { name: pal-secrets }
          env:
            - { name: APP_ENV, value: "production" }
            - { name: APP_NAME, value: "PAL Platform" }
            - { name: STORAGE_DRIVER, value: "local" }
          volumeMounts:
            - { name: uploads, mountPath: /var/www/html/uploads }
          livenessProbe:
            httpGet: { path: /healthz, port: 80 }
            initialDelaySeconds: 20
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /readyz, port: 80 }
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests: { cpu: "100m", memory: "192Mi" }
            limits:   { cpu: "1",    memory: "512Mi" }
      volumes:
        - name: uploads
          persistentVolumeClaim: { claimName: pal-uploads }
---
apiVersion: v1
kind: Service
metadata:
  name: pal-platform
spec:
  selector: { app: pal-platform }
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pal-platform
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "40m"
spec:
  rules:
    - host: pal.your-domain.gov
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pal-platform
                port: { number: 80 }
